

<ul class="list-unstyled text-small">
    <div class="col py-2 mt-1 border bg-white">
        <li>
            <h4><%= @email.object %></h4>
            <p><%= @email.body %></p>
        </li>
        <li>
        <button type="button" class="btn btn-info">Marquer comme non-lu</button>
        <%= link_to 'Supprimer email', email_path(email), method: :delete, remote: true, class: "btn btn-danger btn-small" %>
        </li>

    </div>
</ul>